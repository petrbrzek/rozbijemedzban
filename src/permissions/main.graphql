
query attendeeExists($node_id: ID!, $user_id: ID!) {
 	SomeAttendeeExists(filter: {
    id: $node_id,
  	user: {
      id: $user_id,
    }
  })
}

query isAdmin($user_id: ID!) {
  SomeUserExists(filter: {
    id: $user_id
    admin: {
      user: {
        id: $user_id
      }
    }
  })
}
